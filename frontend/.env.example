# Local: leave empty to use Vite proxy (/api -> localhost:5000)
# Vercel all-in-one deploy: leave empty (same-origin /api)
# Split deploy only: set full backend origin, e.g. https://xxx.onrender.com
VITE_API_URL=
